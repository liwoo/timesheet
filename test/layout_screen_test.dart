import 'package:flutter_test/flutter_test.dart';
//import 'package:flutter/material.dart';
//import 'package:timesheet/layout/constants.dart';
//import 'package:timesheet/layout/root_screen.dart';

void main() {
  testWidgets('it shows the home screen by default', (WidgetTester tester) async{
    expect(true, true);
//    await tester.pumpWidget(new MaterialApp(home: RootScreen()));
//
//    expect(find.byWidget(new BottomNavigationBarItem()), findsOneWidget);
//    expect(find.text('Home'), findsOneWidget);
//    expect(find.text(strings[appStrings.emptyEntries]),
//        findsOneWidget);
  });
}